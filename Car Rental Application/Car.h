#ifndef CAR_H
#define CAR_H
#include <string>
using namespace std;

// Structure for car details
struct Car {
    int id;
    string model;
    bool available; // true if car is available for rent
};

